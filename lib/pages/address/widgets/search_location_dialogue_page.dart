import 'package:flutter/material.dart';

class LocationDialogue extends StatelessWidget {
  const LocationDialogue({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
